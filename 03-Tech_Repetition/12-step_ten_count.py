# Contagem pulada

for z in range(0,110,10):# No Range(início,fim,pulo)
  # O "z" no loop é uma variável que a linguagem atribui um valor automaticamente, não precisa criar uma variável "z = 0" lá em cima
  print(z)
